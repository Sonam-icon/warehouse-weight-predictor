# Dataset

`FMCG_data.csv` is the dataset supplied for the FMCG Supply Optimization challenge.

The repository package contains the file so the notebook can be reproduced locally. **Before making this repository public, verify that the challenge/data terms allow redistribution of the CSV.** If redistribution is not permitted, remove the CSV before publishing; the notebook will still show the expected filename/path.
